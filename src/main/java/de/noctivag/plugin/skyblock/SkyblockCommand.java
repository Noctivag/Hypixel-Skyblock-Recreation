package de.noctivag.plugin.skyblock;
import org.bukkit.inventory.ItemStack;

// The original SkyblockCommand file previously included an inner IslandCommandImpl
// which conflicted with the new implementation. That inner class was removed to
// allow the central `IslandCommandImpl` in this package to be used instead.
// The remaining SkyblockCommand logic can be reintroduced here as needed.
